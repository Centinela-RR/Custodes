name: Auto Pull Request
on:
  push:
    branches:
      - logos
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout the branch
      #- name: checkout
      #  uses: actions/checkout@v2

      - name: Setup git config
        run: |
          # setup the username and email
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"

      - name: Check changed files and commit count
        #Debug print lol
        run: |
          changedFiles=$(git diff --name-only ${GITHUB_SHA}^ ${GITHUB_SHA} | grep '^assets/')
          echo "Changed Files: ${changedFiles}"
          commitCount=$(git rev-list --count ${GITHUB_EVENT_BEFORE}..${GITHUB_SHA})
          echo "Commits Count: ${commitCount}"

      - name: Create Pull Request
        run: |
          if [[ $changedFiles -ge 10 || $commitCount -ge 10 ]]; then
            echo "Creating pull request..."
            gh pr create -B app-prealpha -H logos --title "Automated Logos Pull Request" --body "Changed Files:\n$changedFiles\n\nCommits Count: $commitCount"
          else
            echo "Conditions not met. No pull request created."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}