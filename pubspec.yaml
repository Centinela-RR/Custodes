name: custodes
description: "Aplicación para la seguridad vial."
publish_to: "none"
version: 0.0.3+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6

  # Firebase stuff
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.3
  cloud_firestore: ^4.15.3
  shared_preferences: ^2.2.2

  # Splashscreen - easy to implement
  another_flutter_splash_screen: ^1.2.0

  # Cool page transitions
  page_transition: ^2.1.0
  uuid: ^4.3.3

  # Mapa
  open_street_map_search_and_pick: ^0.1.1
  proj4dart: ^2.1.0

  url_strategy: ^0.2.0
  vector_math: ^2.1.4
  flutter_map_geojson: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
